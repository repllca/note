import React, { useRef } from 'react';
import { Canvas, useFrame } from '@react-three/fiber';
import * as THREE from 'three';
import CanvasComponent from './components/CamvasComponent';
import './App.css'; // CSSファイルをインポート
const App = () => (
  <CanvasComponent/>

);

export default App;


